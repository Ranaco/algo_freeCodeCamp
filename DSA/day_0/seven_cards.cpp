#include <iostream>
#include<list>
#include <map>
#include <string> 
#include <utility>

using namespace std;

int locate_card(list<int> cards, int query){
  
  return 0;
}

int main(){

  map<string, map<string, list<int>>> test_one;

  test_one.insert(pair<string, map<string, list<int>>>("input", 
      )
    );

  list<int> cards = { 13, 11, 10, 7, 4, 3, 1, 0 };
  int query = 7;
  locate_card(cards, query);
  return 0;
}
